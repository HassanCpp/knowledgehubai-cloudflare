const ImageProcessor = require('./ImageProcessor');

class InvoiceProcessor extends ImageProcessor {
  /**
   * InvoiceProcessor inherits vision extraction logic from ImageProcessor.
   * Can be customized with custom heuristics or parsers for structural invoice schemas.
   */
}

module.exports = InvoiceProcessor;
